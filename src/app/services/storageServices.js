export const saveLastInterestProduct = (productName) =>{
    //API Locastorage / sessionStorage 
    //Registro asociado a un valor
    sessionStorage.setItem("lastProduct", productName);
    // sessionStorage
}